{
  "name": "voice-assistant",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "server:install": "cd server && npm install",
    "server:download-models": "cd server && npm run download-models",
    "server:dev": "cd server && npm run dev",
    "setup": "npm install && npm run server:install && npm run server:download-models"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
